// Kept as a compatibility alias for existing clients. New callers should use
// /api/voice/query; both paths enforce the same authenticated, grounded flow.
export { POST } from '../voice/query/route';
